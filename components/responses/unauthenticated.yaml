description: "Unauthenticated"
summary: "Unauthenticated"
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          example: "Please try to login before executing this action."
