type: object
properties:
  current:
    type: string
    example: "https://api.example.com/"
  next:
    type: string
    example: "https://api.example.com/"
  prev:
    type: string
    example: "https://api.example.com/"
  last:
    type: string
    example: "https://api.example.com/"
  first:
    type: string
    example: "https://api.example.com/"
  total:
    type: integer
    example: 100
