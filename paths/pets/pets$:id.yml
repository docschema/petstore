get:
  responses:
    "200":
      summary: "Show details about a pet"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: "https://raw.githubusercontent.com/stubleapp/petstore/main/components/schemas/pet.yml"
